# Jido Integration V2 Consumer Surfaces

Runtime support for generated `Jido.Action`, `Jido.Sensor`, and `Jido.Plugin`
surfaces derived from authored integration manifests.

This package exists so runtimeful generated-surface behavior does not have to
keep growing inside the pure contracts package.
