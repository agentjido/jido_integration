defmodule Jido.Integration.V2.Connectors.MarketData.Handler do
  @moduledoc """
  Marker module for the authored Market Data stream surface.

  Runtime execution is owned by Runtime Control plus ASM rather than connector-local
  stream callbacks.
  """
end
